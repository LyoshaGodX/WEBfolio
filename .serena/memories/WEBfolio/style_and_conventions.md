Наблюдения и предложения по стилю и соглашениям:

Найдено:
- Проект на Python (PEP8 — стандарт для Python).
- Нет явных файлов конфигурации для стиля/линтинга (`pyproject.toml`, `.flake8`, `.editorconfig` не обнаружены на верхнем уровне).

Рекомендации (предложение, согласуйте с автором):
- Форматирование: использовать `black` (автоматически форматирует код).
- Сортировка импортов: `isort`.
- Линтинг: `flake8` с базовым набором правил, опционально `pylint`.
- Типизация: добавить `mypy` при желании и постепенно аннотировать критичные модули.
- Докстринги: следовать Google- или NumPy-стилю (по согласованию).

Если вы хотите, могу добавить конфигурации (`pyproject.toml`, `.flake8`) и выполнить первичное форматирование/линтинг.